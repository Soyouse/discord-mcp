# NO-OP VOLONTAIRE — le gate (tests + mutation) est DÉPLACÉ en CI async GitHub Actions.
# Le push ne BLOQUE PLUS (fin du « 40 ans à chaque push »). La CI rejoue tout en arrière-plan sur les serveurs GitHub
# (cache npm + Stryker INCRÉMENTAL + jobs parallèles + cancel-in-progress) → détection post-push, zéro attente locale.
# NE PAS y remettre `npm test`/`npm run mutation`. Dev local = `npm run test:watch`. Doctrine = memory feedback_ci_fast_feedback_never_block.
